{
  "name": "bypass",
  "version": "0.1",
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore"
  },
  "dependencies": {
    "express": "^4.17.1",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "eslint": "^6.6.0",
    "eslint-config-strongloop": "^2.1.0"
  }
}
